[build]
  command = "npm run build"  
  publish = "client/dist"    
  
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

[functions]
  directory = "server/functions"  # Diretório das funções serverless
